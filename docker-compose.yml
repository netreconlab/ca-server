version: "3"
services:
  ca-app:
    build: .
    restart: always
    working_dir: /app
    volumes:
      - ./server:/app/server
    command: poetry run uvicorn server.main:app --host 0.0.0.0 --port 3000
    ports:
      - '3000:3000'
